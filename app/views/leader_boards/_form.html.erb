<%= form_for(@leader_board) do |f| %>
  <% if @leader_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leader_board.errors.count, "error") %> prohibited this leader_board from being saved:</h2>

      <ul>
      <% @leader_board.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game %><br />
    <%= f.text_field :game %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
